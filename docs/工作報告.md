# AI 助教 API 故障診斷與多核心備援優化工作報告

本報告總結了針對「AI 機器學習助教」聊天機器人 API 的故障排查、解決方案設計、防禦性開發，以及最終多核心備援與自動降級機制的實作成果。

## 1. 核心問題排查與診斷

在開發與測試階段，AI 助教面臨了幾項嚴峻的 API 錯誤：
1. **OpenAI API 429 Quota Exceeded**：原帳號的免費 API 額度過期或用盡，導致服務不可用。
2. **Gemini API 429 Rate Limits**：受限於 Google AI Studio 免費版每分鐘的呼叫頻率限制（RPM），或受部署伺服器（Render）IP 地區限流。
3. **OpenRouter API 401 Unauthorized**：部署平台的環境變數在設定時常因多餘引號（如 `"sk-or-v1-..."`）或空格，造成金鑰解析錯誤與 Authentication header 遺失。
4. **OpenRouter API 404 Model Unavailable**：指定之 `meta-llama/llama-3-8b-instruct:free` 免費模型突然被下架或改為收費版。

---

## 2. 解決方案與優化設計

為確保 Demo 與部署階段 100% 不中斷，本專案重構了後端核心對話架構，設計了以下四層保障：

### A. 防禦性金鑰淨化機制 (Sanitization)
針對部署環境變數解析問題，實作了防禦性清洗函數：
* 自動清除金鑰字串的前後空白。
* 自動過濾不慎填入的單引號 (`'`) 與雙引號 (`"`")。
* 新增啟動遮罩日誌（如 `sk-or-v1...abcd (length: 53)`），方便在 Render Log 中快速核對金鑰狀態。

### B. 動態免費路由模型 (OpenRouter Free Router)
將 OpenRouter 指定模型改為 **`openrouter/free`**。此 ID 會由 OpenRouter 官方動態分配當下可用的免費開源大模型（如 Llama 3、Qwen、Gemma 等），避免模型下架引起的 404 崩潰。
* ⚠️ **特別提醒**：由於使用的是 OpenRouter 的**免費額度**，此管道可能受到官方的速率限制（Rate Limits，通常為 20 RPM/200 RPD），在尖峰時段回應速度可能會較慢或稍有延遲。

### C. 鏈式自動故障轉移 (API Failover Sequence)
重構對話接口，將多種 AI API 串聯成一條鏈。當使用者發問時，後端會**依序嘗試**：
$$\text{Gemini 3.5 Flash} \longrightarrow \text{Groq Llama 3} \longrightarrow \text{OpenRouter Free} \longrightarrow \text{OpenAI GPT-4o-mini}$$
若鏈條上的某個 API 出現異常（如 429、401、404 等），系統會立即捕獲異常，向前端傳送轉移提示（`*[系統提示]* {Provider} 失敗，正在自動切換備用方案...`），並**立刻調用下一個備用 AI**，無縫繼續輸出。

### D. 終極模擬模式降級 (Fallback to Mock Mode)
如果上述所有的 AI API 皆呼叫失敗（或皆未配置），後端會自動無縫降級至 **模擬演示模式 (Mock Mode)**，繼續以打字機效果輸出精心設計的機器學習教學邏輯（可引導學生回答「過擬合」、「資料洩漏」及「建模流程」等關鍵字問題），保證展示過程絕對不會呈現崩潰白畫面。

### E. 前端小測驗作答狀態防呆鎖定 (Quiz State Lock)
針對前端「小測驗」面板中，學生一旦答對後，再次點擊其他錯誤選項會使答題狀態跳回「再想一下」並錯誤變更狀態的邏輯缺陷進行了修正：
* 在 `selectAnswer` 函數中，新增了 `if (answers[algoId] === true) return;`。
* 當該演算法的測驗已正確回答（狀態為 `true`）後，任何後續的點選皆會被直接忽視，鎖定「答對了」狀態，保障學習成果的 UI 呈現不會受二次誤點干擾。

---

## 3. 開發指令與進度紀錄
詳細的開發步驟與提交指令，請參閱：
* 📄 [指令執行紀錄](log.md)

---

## 4. 後端環境變數配置說明 (Render / .env)
若要啟用特定 AI，請在 `.env` 或雲端平台環境變數中設定以下對應項目：

| 環境變數鍵名 | 來源平台 | 推薦模型 | 說明 |
| :--- | :--- | :--- | :--- |
| `GEMINI_API_KEY` | Google AI Studio | `gemini-3.5-flash` | 免費且最推薦的主力模型 |
| `GROQ_API_KEY` | Groq Console | `llama3-8b-8192` | 免費、速度最快（首選備援） |
| `OPENROUTER_API_KEY` | OpenRouter | `openrouter/free` | 動態免費模型備援 |
| `OPENAI_API_KEY` | OpenAI Platform | `gpt-4o-mini` | 付費備援 |

---

# 全端審查與元件重構工作報告（2026-06-08）

本報告記錄以全端工程師暨 UX/UI 設計師視角，對「ML 機器學習十大演算法互動學習平台」進行的全面審查、立即修復及中長期重構成果。

## 1. 全端審查問題分類

透過完整讀取 `backend/main.py`、`frontend/pages/index.js`（2099 行）、`frontend/components/AIChatbot.tsx` 等核心檔案，歸納出以下問題：

### 🔴 立即問題（安全 / 功能缺陷）

| 項目 | 問題描述 | 影響 |
| :--- | :--- | :--- |
| CORS 開放 `*` | `allow_origins=["*"]` 允許任意來源跨域請求 | 安全風險 |
| WebSocket 即時連線 | Chatbot 元件掛載即建立 WS，視窗未開啟也在連線 | 浪費伺服器資源 |
| Quiz 進度不持久 | `answers` 僅存 React state，重新整理即清空 | 學習體驗中斷 |

### 🟡 近期問題（UX 體驗缺口）

| 項目 | 問題描述 |
| :--- | :--- |
| SVG 動畫無說明 | 視覺化動畫切換但無文字解說，使用者不知道看什麼 |
| AI Provider 切換訊息技術化 | 顯示「切換至 Groq」，一般使用者看不懂 |

### 🟢 中長期問題（可維護性）

| 項目 | 問題描述 |
| :--- | :--- |
| `index.js` 2099 行 | 所有元件、資料、樣式混在一個檔案，極難維護 |
| 演算法資料三份並存 | `meta`（index.js）、`ALGORITHMS`（main.py）、`reportInsights`（algorithmReport.js）三份各自為政 |

---

## 2. 立即修復成果

### 2.1 CORS 安全限縮
將 `backend/main.py` 的 CORS 設定從 wildcard 改為讀取環境變數 `ALLOWED_ORIGINS`。同步更新 `render.yaml` 新增對應欄位，部署時在 Render Dashboard 填入前端實際網域即可生效。

**變更檔案：** `backend/main.py`、`render.yaml`

### 2.2 WebSocket Lazy Connect
重寫 `AIChatbot.tsx` 的連線邏輯：
- 移除「元件掛載即連線」的行為
- 改為「使用者第一次開啟聊天視窗」才建立 WebSocket 連線
- 斷線重連計數器從 `useState` 改為 `useRef`，避免不必要的重新渲染
- 新增 `isMountedRef` 防止元件 unmount 後的 state 更新

**變更檔案：** `frontend/components/AIChatbot.tsx`

### 2.3 Quiz 進度 localStorage 持久化
- `answers` state 改為 lazy initializer，從 `localStorage` 讀取初始值
- 新增 `useEffect` 監聽 `answers` 變化，同步寫入 `localStorage`
- key 名稱：`ml-quiz-answers`（與現有 `ml-learning-scene` 命名風格一致）

**變更檔案：** `frontend/pages/index.js`

---

## 3. 近期修復成果

### 3.1 SVG 動畫說明文字
新增 `animationDescriptions` 資料物件，為 10 種演算法的每個動畫幀（共 50 個幀）各自撰寫中文說明。視覺化面板在動畫下方顯示「步驟 N/5」標籤與該幀的教學描述，使動畫由裝飾性元素升級為有實質教學效果的互動內容。

**變更檔案：** `frontend/lib/algorithmData.js`、`frontend/components/VisualPanel.jsx`

---

## 4. 中長期重構成果：元件拆分

### 4.1 重構前後對比

| 指標 | 重構前 | 重構後 |
| :--- | :--- | :--- |
| `index.js` 行數 | **2099 行** | **950 行**（縮減 55%） |
| 新建元件數 | 0 | **6 個** |
| 資料常數位置 | 散落 index.js | 集中 `lib/algorithmData.js` |

### 4.2 新建檔案清單

| 檔案 | 職責 |
| :--- | :--- |
| `frontend/lib/algorithmData.js` | 演算法顯示 metadata、動畫說明、程式碼範例、執行結果、工具函數 |
| `frontend/components/MiniChart.jsx` | 10 種演算法 SVG 動畫元件，各自有 5 個動畫幀 |
| `frontend/components/HeroIllustration.jsx` | Hero 區塊的筆電插圖與浮動卡片 |
| `frontend/components/VisualPanel.jsx` | 互動式視覺化面板（含動畫說明文字） |
| `frontend/components/CodePanel.jsx` | 實作範例面板（程式碼展示與執行） |
| `frontend/components/QuizPanel.jsx` | 小測驗面板（選項 + 作答狀態） |
| `frontend/components/DetailModal.jsx` | 演算法完整說明 Modal |

### 4.3 CSS 架構策略
- 各元件自帶 `<style jsx>` 管理元件特有樣式
- 共用樣式（`.panel`、`.panelHeader`、`.chips`）在 `index.js` 以 `:global()` 宣告，確保提取後的元件也能正確繼承
- CSS 自訂屬性（`--accent`、`--surface` 等）透過 DOM 串聯自然傳遞，子元件無需額外設定

---

## 5. 後續待辦（截至第六階段）

以下為已識別但尚未處理的中長期項目：

- [x] **演算法資料統一來源**：✅ 已於第七階段完成
- [ ] **AI Provider 切換訊息優化**：將技術性的「切換至 Groq」改為對用戶友善的提示語，或完全靜音
- [ ] **手機版 RWD 驗證**：系統性測試 Mobile 排版，補強 breakpoint 設計
- [ ] **React Error Boundary**：為各面板加入錯誤邊界，防止單一元件錯誤導致整頁崩潰

---

# 演算法資料統一、CORS 診斷與進度追蹤實作工作報告（2026-06-08）

本報告記錄第七階段的三項主要工作：演算法資料合併為單一來源、Render 部署 CORS 問題診斷，以及前端介面優化（移除冗餘區塊、實作真實進度追蹤）。

## 1. 問題分類

### 🟢 中長期（可維護性）
| 項目 | 問題描述 |
| :--- | :--- |
| 演算法資料三份並存 | `meta`（algorithmData.js）、`ALGORITHMS`（main.py）、`reportInsights`（algorithmReport.js）各自維護，資料修改需同步三處 |

### 🔴 部署問題（環境設定）
| 項目 | 問題描述 | 影響 |
| :--- | :--- | :--- |
| Render CORS 未設定 | `ALLOWED_ORIGINS` 環境變數未填入前端網域，導致 API 完全無法存取 | 前端白畫面 |

### 🟡 UX 問題
| 項目 | 問題描述 |
| :--- | :--- |
| 實作索引區塊冗餘 | 與上方演算法卡片完全重複，徒佔版面 |
| Dead buttons | 學習路徑、進度追蹤按鈕點擊無任何反應 |

---

## 2. 演算法資料統一來源

### 2.1 架構變更

**合併前（三份各自為政）：**
```
backend/main.py                → 基礎資料（描述、優缺點）
frontend/lib/algorithmData.js  → 顯示 meta（名稱、顏色、測驗）
frontend/lib/algorithmReport.js → 報告洞察（流程、指標、陷阱）
```

**合併後（單一來源）：**
```
backend/main.py → 完整資料（20 個欄位）
                  GET /api/algorithms 一次返回所有前端所需資訊
```

### 2.2 新增 Pydantic 模型

新增 `QuizItem` 巢狀模型，`Algorithm` 模型擴展至 20 個欄位，涵蓋原三份資料的所有內容。quiz 從前端的陣列格式（`[question, options, correctIndex]`）改為具名物件（`{ question, options, correctIndex }`），提升可讀性。

### 2.3 前端清理成果

| 檔案 | 變更 |
| :--- | :--- |
| `backend/main.py` | 新增 QuizItem 模型，Algorithm 擴展 14 個欄位，10 筆 ALGORITHMS 補齊完整資料 |
| `frontend/lib/algorithmData.js` | 移除 `meta` 物件（-11 行），移除 `enrich()` 函數 |
| `frontend/lib/algorithmReport.js` | 移除 `reportInsights`（-82 行），只保留 `reportGuide` |
| `frontend/components/QuizPanel.jsx` | `quiz[0/1/2]` → `quiz.question/.options/.correctIndex` |
| `frontend/pages/index.js` | 移除 meta/reportInsights 引用；`activeInsight = active` |
| `frontend/pages/algorithms/[id].js` | 直接讀 API 欄位，完全移除 reportInsights 依賴 |

**Commit：** `1a4d507` — 6 個檔案，172 行新增，216 行刪除

---

## 3. Render 部署 CORS 診斷

### 問題根因

上一階段將 CORS 從 `allow_origins=["*"]` 改為讀取環境變數 `ALLOWED_ORIGINS`，但未在 Render Dashboard 後端服務設定該變數，導致預設值只開放 `localhost:3000`，生產環境完全封鎖。

### 修復方式（無需改程式碼）

在 Render Dashboard 後端服務的 **Environment** 頁面新增：

```
ALLOWED_ORIGINS = https://ml-algorithm-learning.onrender.com
```

同時確認前端服務已設定：
```
NEXT_PUBLIC_API_BASE_URL = https://mlalgorithmlearning.onrender.com
NEXT_PUBLIC_WS_URL       = wss://mlalgorithmlearning.onrender.com/ws/ai-chat
```

---

## 4. 前端介面優化

### 4.1 移除冗餘元素

| 移除項目 | 原因 |
| :--- | :--- |
| 「所有演算法實作索引」section | 與上方演算法卡片功能完全重複，無附加價值 |
| 「學習路徑」按鈕 | 空殼 dead button，點擊無任何反應 |
| Sidebar `#examples` 錨點連結 | 對應 section 已刪除 |

### 4.2 進度追蹤功能實作

將 dead button 升級為真實功能：點擊「進度追蹤」切換展開/收合面板。

**面板功能：**
- 10 個演算法各顯示一格，含識別顏色圓點
- `✓` 綠底 = 答對；`✗` 紅底 = 答錯過；`─` = 未作答
- 點擊任何格子 → 跳至該演算法學習區 + 自動收合面板
- 頂部顯示「N / 10 完成」即時計數
- 按鈕按下時顯示 accent 顏色（active 狀態視覺回饋）

**Commit：** `c8215cf` — 1 個檔案，95 行新增，35 行刪除

---

## 5. 後續待辦

- [ ] **AI Provider 切換訊息優化**：將技術性的「切換至 Groq」改為對用戶友善的提示語，或完全靜音
- [ ] **手機版 RWD 驗證**：系統性測試 Mobile 排版，補強 breakpoint 設計
- [ ] **React Error Boundary**：為各面板加入錯誤邊界，防止單一元件錯誤導致整頁崩潰
- [ ] **PDF 補充內容**：CRISP-DM 方法論（p.29）、Ridge/Lasso 補充、Gaussian/Bernoulli Naive Bayes 變體尚未整合至平台內容

---

# 教育功能擴充 + 線性迴歸互動實驗室工作報告（2026-06-09）

本報告記錄第八階段的四項主要工作：教育內容深化、Render 冷啟動 UX 改善，以及線性迴歸模擬實驗室的設計與迭代。

## 1. Phase 2 教育功能擴充

### 1.1 小測驗系統升級（QuizPanel.jsx、backend/main.py）

| 改動 | 說明 |
| :--- | :--- |
| 題數 1 → 3 | 每個演算法由 1 題升至 3 題，難度由易到難 |
| 解釋文字 | 每題加入 `explanation`，答對答錯皆顯示學習說明 |
| 進度圓點 | 底部新增 3 個圓點：綠=答對、紅=答錯、白=未作答 |
| 自由導航 | 點擊圓點可跳至任意一題 |
| 重新測驗 | 全部答對後出現「重新測驗」按鈕，可清空重來 |
| 次題按鈕 | 答題後顯示「下一題 →」按鈕 |
| 儲存格式 | localStorage key 從 `ml-quiz` 升版至 `ml-quiz-v2`，格式改為 `{ [algoId]: Array<{selected, correct}|null> }` |

### 1.2 視覺化注釋（VisualPanel.jsx、algorithmData.js）

- 新增 `chartLegend`：每個演算法的圖表顏色/形狀說明（10 組）
- 新增 `mathFormulas`：10 個演算法各一條核心公式 + 白話描述
- VisualPanel 在動畫下方依序顯示圖例區塊與公式框

### 1.3 推薦學習路徑

- 首頁新增「推薦學習路徑」section（入門 → 中階 → 進階）
- 每張演算法卡片加入難度徽章（入門/中階/進階）
- Lab 區塊下方新增「下一演算法 →」按鈕，引導學習順序

---

## 2. Render 冷啟動 UX 改善（frontend/pages/index.js）

### 問題背景

Render 免費方案後端閒置 15 分鐘後自動暫停。前端啟動時若後端尚未就緒，`GET /api/algorithms` 失敗，過去顯示技術性錯誤訊息「目前無法讀取資料，請確認後端 API 是否已啟動」。

### 解決方案

設計三段式狀態處理：

| 狀態 | 畫面 |
| :--- | :--- |
| `loading=true`，無資料 | 全頁旋轉動畫 + 「平台啟動中，請稍候…」 |
| fetch 失敗（`error='warming-up'`） | 雲圖示 + 「後端服務喚醒中，約需 30~60 秒」+ 立即重試按鈕 |
| 後端就緒 | 自動載入完整平台，不需使用者操作 |

- 自動重試：catch 後啟動 `setInterval(10000)`，後端就緒時自動清除 timer 並渲染資料
- 兩個畫面均繼承 light/dark 主題色彩變數
- 舊的通用錯誤訊息列已移除

---

## 3. 線性迴歸互動實驗室（LinearRegressionLab.jsx + backend/main.py）

### 3.1 後端新增 API

| 端點 | 說明 |
| :--- | :--- |
| `POST /api/run-linear-regression` | 接收使用者提供的 (x, y) 點，回傳斜率、截距、R²、預測值 |
| `POST /api/simulate-linear-regression` | 接收 n, a, b, σ² 四個參數，生成模擬資料並回傳完整迴歸結果 |

`simulate-linear-regression` 回傳內容：
- 生成的 `x_values`, `y_values`（`x ~ Uniform(-100, 100)`，`y = ax + b + N(0, sqrt(σ²))`）
- `fitted_slope`、`fitted_intercept`、`r_squared`、`rmse`
- `true_line_y`（真實線端點）、`fitted_line_y`（迴歸線端點）
- `outlier_indices`（殘差最大的前 10 個點的索引）、`residuals`

### 3.2 迭代歷程

**第一版（滑桿輸入資料點）：** 使用者可對每個 (x, y) 點拖動滑桿，按送出計算。使用者回饋希望改為「生成模擬資料」的方式。

**第二版（模擬實驗室）：** 改為 4 個參數滑桿：

| 滑桿 | 範圍 | 說明 |
| :--- | :--- | :--- |
| n（資料點數量） | 10 ~ 300 | 生成隨機點數 |
| a（真實斜率） | -50 ~ 50 | 控制直線傾斜 |
| b（真實截距） | 0 ~ 100 | y 軸截距 |
| σ²（噪聲變異數） | 0 ~ 300 | 資料散亂程度 |

公式框即時顯示 `y = ax + b + N(0, σ²)` 隨參數更新。

### 3.3 SVG 圖表設計

| 元素 | 視覺說明 |
| :--- | :--- |
| 灰色小圓點 | 生成的資料點（非離群） |
| 藍色虛線 | 真實線（`y = ax + b`） |
| 紅色實線 | 迴歸線（模型擬合結果） |
| 黃色圓圈 + 數字 | 前 10 大殘差離群點，圓點內印排名數字 1–10 |
| 黃色垂直線 | 離群點到迴歸線的殘差落差線 |

### 3.4 UI 迭代修正

**問題 1（圖表過大遮住輸入欄）：**
- 新增 `maxHeight: 300px` 限制 SVG 高度
- 離群點圓圈 r: 7 → 10，加外光暈（r=15, 18% opacity）

**問題 2（仍遮住輸入、希望圖表為正方形、離群點看不清）：**
- `lrLab max-width: 780px`，grid 欄位改為固定 `300px 420px`
- SVG viewBox `540×360` → `420×420`（正方形）
- 離群點顏色 orange → 亮黃 `#facc15`（深色邊框 `#1e1e1e`），在深色/淺色背景皆高對比

---

## 4. 新增套件依賴

```
scikit-learn   # LinearRegression 模型
numpy          # 隨機數生成、矩陣運算、殘差計算
```
已加入 `backend/requirements.txt`，Render 重新部署時自動安裝。

---

## 5. 後續待辦（第八階段後）

- [ ] 將線性迴歸實驗室擴展至其他演算法（如 KNN、K-Means）
- [ ] 增加「收斂動畫」：逐步顯示梯度下降過程
- [ ] Phase 3：新手導覽（Onboarding Tour）、演算法關係圖
- [ ] Phase 4：完整 Real Code Execution（使用者可調整超參數）
